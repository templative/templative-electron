import React from 'react';

const CreatingAComponent = () => {
    return (
        <>
            <p>A Templative project is made up of a collection of compositions. Each composition is a unique thing in your game, like a box, die, or deck.</p>
            <p>Templative supports both custom components (like decks of cards) and stock components (like plain dice).</p>
        </>
    );
};

export default CreatingAComponent;
